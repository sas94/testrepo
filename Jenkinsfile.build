pipeline {
   agent any
  	  stages {
	       stage('hello') {
		      steps {
			      echo 'hello world'
				 }
			}
		}
    post {
        success {
           build job: "/batch-deploy/" + env.BRANCH_NAME.replaceAll("/", "%2F")
            
        }
     }
}
