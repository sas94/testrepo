pipeline {
   agent any
  	  stages {
	       stage('hello') {
		      steps {
			      echo 'hello world'
				 }
			}
		}
    post {
        success {
            script {
                build job: = "/batch-deploy/" + env.BRANCH_NAME.replaceAll("/", "%2F")
            }
        }
     }
}
